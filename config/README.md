# config/

YAML configuration files for training runs.

    File                    Used by
    paths_config.yaml       Global path config (model, data, checkpoint paths)
    sft_single_stage.yaml   Config for single-stage SFT training run
